# grokImage 2026-07 维护日志

## 2026-07-15

- 目标：完善图片管理、降低 Web 并发风险并处理 107 个 Build 异常账号。
- 观察到的事实：Panda 单 Webshare 节点连接正常，Grok 返回 Cloudflare 403；Build 异常由 87 个 `invalid_grant` 和 20 个 `access denied` 组成，只有 2 个 invalid_grant 账号已有 Web 关联。
- 已完成：图片日期筛选/分组/按日删除/全部删除；秒级时间、生成耗时、实际和请求分辨率；Web/Web Asset 单并发闸门；已关联失效 Build 的 Web→Build 修复路径；账号状态操作提示；维护文档实化。
- 验证：`cd backend; go test ./...` 通过；`cd frontend; pnpm lint` 与 `pnpm build` 通过；本地临时实例验证图片列表、日期筛选、图片读取、元数据和按日期删除通过；Panda 只执行了只读状态分析和单代理请求 canary。
- 生产结果：镜像 `sha256:719a85035355...` 已部署，页面显示日期分组、秒级时间和旧图片实际尺寸；Panda 部署后约 30–53 MiB，主服务持续 healthy。
- Build 结果：2 个已有 Web 关联的 invalid_grant 账号已恢复并同步；当前 Build 为 97 active、85 invalid_grant、13 access denied。
- Web canary：Webshare 连接正常，但单浏览器加载 Grok 页面超时，后端 502；桥接已停止，未运行全量刷新。已在本地增加 30 秒启动上限、阶段错误和结构化 502。
- 未完成：剩余 Web 账号仍受 Cloudflare 出口会话阻塞；85 个 Build 账号等待新 RT，13 个等待具备 Build 权限的授权。
- 下一步：部署桥接诊断增强；更换住宅/ISP 出口后再做单账号 canary。
- 关联文件：`backend/internal/application/media/`、`backend/internal/infra/egress/`、`backend/internal/application/account/`、`frontend/src/features/media/`、`frontend/src/features/accounts/`。
