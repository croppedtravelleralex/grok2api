# grokImage 2026-07 维护日志

## 2026-07-25 / 07-26

- **架构共识**：[20](../20-ticket-ready-slot-dispatch-merge-2026-07-25.md) — 票池逻辑并入号池（TicketReady / 销票槽位 SlotRegistry）；四套集合（dispatch / pin / runtime / 票）漂移根因与 P0–P2 实施阶段。
- **Panda 实测**：available ~19 票；pin 3（250/263/342）；runtime 曾缩至 1；真孤儿（有票 ∉ dispatch）~6；「16 不在 pin」≠「16 掉 dispatch」。
- **dispatch 规律**：无闲置 TTL；Imagine `SyncedAt` 30min 过期或探针失败（15min 冷却）可未发图即出池；票不随号迁移 → 孤儿票。
- **工具**：`chrome_ticket_pool_probe.py --remediate`、`panda_unified_pool_snapshot.py`、`chrome_ticket_probe_rs/`、`chrome_ticket_jit_mint_concurrent.py`（部分未 commit）。
- **disableCooldown**：`routing.disableCooldown` 默认 true；Panda `disable-cooldown-v9` 二进制（待 GHCR）；DB 已清 `cooldown_until`。
- **文档同步**：02/04/05/13/plan/README + BE-024 backlog。

## 2026-07-24

- **阶段 A 落地**：`96b664d` — BE-018 egress 流量、BE-019 pin∩dispatch、BE-020 selection_reason、BE-021 有票优先 + mint daemon 池深。
- **阶段 B smoke**（镜像 `21380ef5…0584`，pin 1574/1507/1467/92）：`pinNotInDispatch=[]`，`dispatchImageLen=4`；**10× 探针 503=0**；**10× 生图 0/200**（6×429，4×502）。egress 审计 API smoke 路径 404。
- **文档**：[17](../17-web-four-pool-and-imaging-success-rates-2026-07-24.md) — 纯 HTTP vs 开票成功率对照、开票职责边界、Web 四池设计。
- **结论**：503 已解；E2E 瓶颈在账号额度/soft_stop + 调度准入过宽，非票失效。下一步 BE-023 四池。
- **R-delay 全档 pass**（1574）：0/1m/5m/30m，同 meta re-push 二次均 200+pool_hit。
- **S 存活**：10m/30m pass（1507）；**60m pass（1574）→ 当前证据票至少活 60min**；15m 曾 503 抖动。S-3h/6h/12h 已 mint 挂起（3h 于 13:08 到期待验收）。
- **V 串行**：round 1–4 pass（1574/92/1507/1467）；round 5 **502+pool_hit** 停；V-conc 未跑。
- **票池语言**：生产票池为 **Go**，非 Rust。
- **10 并发**：槽位已部署，**稳定 10 并发未验收**。
- 排障：udeal 出口冷却（非票问题）、pin∩图池索引、88 探针 429。详情 [16](../16-chrome-ticket-experiments-round2-2026-07-23.md)。
- **udeal 出口政策与统计**：禁止旋转池；冷却算法 + 72h/48h 失败分层写入 [07](../07-udeal-zero-browser-ops-2026-07-21.md)。
- **双池 + 流量计划**：[plan.md](../plan.md)；**暂停**生图压测直至门禁通过。
- **语言纪律**：Python 仅 PoC；跑通后 Rust 工具 + Go 服务端。BE-022 入 backlog。

## 2026-07-23

- **会话收口**：[Chrome 票池与 Admin 密码](397f17e4-fbe6-4f5b-a416-a5b40b8f642a)；密码调查子任务 [8136e7b9](8136e7b9-276d-40ec-8714-654fcd2df9d5)。
- **部署纪律纠正**：用户发现曾用 tar/scp 大文件 + Panda 上 `go build`；已改回 GitHub Actions/GHCR → Panda 仅 `pull`。asset 下载与 Python 成功路径对齐后发布镜像 `sha256:49f23f31…7a841`（`c07cc2e`），1467 `pool_hit` 后生图 **200**。
- **Chrome 票池运维**：本机持续出票（minter/daemon）；实验改为分批短跑 ≤10min，停用 30–60min 单次长测。文档 [13](../13-chrome-ticket-pool-panda-api-2026-07-23.md)、[14](../14-chrome-ticket-lifecycle-experiments-2026-07-23.md)。
- **第一轮短批（1467）**：S0、D-1m/3m/5m 均 **200 + pool_hit**（跨开票/消费 IP）；R2/C1/C2/M2 多为 429 但 `pool_hit=true` → 票路径成立、账号限速。据此 [15](../15-chrome-ticket-cf403-ip-reframe-2026-07-23.md) 重排 CF403/IP 结论：有票路径不要求开票 IP=消费 IP；429≠票失效。
- **Admin 密码三源不同步**：secrets（7/17）与 DB hash 不一致导致实验 login 401；import 读 `.tmp` 仍 200；reset 脚本另有硬编码口令。23:22 重置并同步三源，login 200。工具 `_panda_reset_admin_password.py`、`_panda_admin_token.py`。
- **第二轮实验**：R-delay-0 多次因 Imagine 429 / Too many requests / 偶发 503 出错即停；一次首 probe **200**、立即复用 **429**。R-delay 其余档、S/V/mint_fast **未跑完**。详情 [16](../16-chrome-ticket-experiments-round2-2026-07-23.md)。日志 `.tmp/chrome-ticket-experiments.jsonl`。
- **下一步**：等 1467 冷却 10–15min 或换未连打 yumail 号，续跑 R-delay → S → V → mint_fast，并补带宽表。

## 2026-07-22

- **纯 HTTP Lite 生产接手完成到真实阻塞边界**：最终镜像 `613a305` / `sha256:5da879fc…15295`，CI run `29901662897` 全绿并部署；API/container healthy，主服务约 39–54 MiB。
- **“两个账号同时生图 429”根因拆分**：单请求也会 `usage_limit_reached` 或 SSE `isSoftStop=true`，证伪“必须双账号并发”；本地 pipeline 429、上游额度 429、soft-stop/502 已分开记录。
- **发现并修复同账号并发复用**：近期成功账号优先曾让两条并发请求同时拿到同一账号，其中一路报 `too many requests in progress`。`52c04ec` 把 Lite 同账号并发固定为 1；生产复验请求已分散到不同账号。
- **SSE 配置漂移修复**：调度器默认已是 `1→1→6`，但应用 wiring 仍硬编码 `2→3→6`；`613a305` 统一从 `DefaultConfig()` 派生。生产 Timeline 确认 slots=10 / queue=100 / Expand=2 / SSE target=1,max=6 / Download=8。
- **账号调度优化**：模型级近期成功优先、未知其次、soft-stop 最后；soft-stop 30s 指数冷却到 5m、成功偏好 30m；Lite 最多 6 个不同账号并以 1/2/4s 退避，显式 429 保留独立 retry budget。
- **signer 漂移**：静态索引 `[31,16,43,8]→[38,33,24,32]`；8 分钟动态 challenge 重算出现 `refresh_ok` 但上游 code 7。一次性 Chrome 捕获 current matched pair 后，浏览器关闭的纯 HTTP text 200/0.88s、Lite 200/5.74s；Panda 仅同步 seed+HEX，未同步抽钥 SSO/Cookie，刷新窗口临时放宽到 24h。
- **最终生产门禁未通过**：fresh signer 已消除 403，但最终两次单请求分别 27.04s、34.97s 后返回真实 429；按规则未运行 2/4/10。代码已支持 10 个客户端请求进入流水线，但生产 10/10 成功仍被现有 20 个 dispatch 账号的 Imagine 有效额度/soft-stop 接受率阻塞。
- **纯 HTTP Lite P0–P2 本地收口**：修复 trace `ssems` 列更新；Statsig 改为每请求新票、只缓存首页 meta；准入/扩写/SSE/下图统一显式 FIFO/aging，取消或异常结束归还容量；Timeline 新增槽 owner、目标阶段、FIFO 位置/等待时长和阶段排队数。
- `expandConcurrency` 已贯通 settings API、持久化、restartRequired 和前端；`generation_timing` 增加 pipeline 四阶段耗时。
- 验证：`go test ./...`、`go vet ./...`、`pnpm lint`、`pnpm build` 通过；race 因本机 MinGW `cc1.exe` 无 64-bit 支持未运行。
- Panda 当前 `4764cf41…` 的失败基线为 80/80 `403 upstream_error`；本轮代码尚未部署，后续严格按 1→2→4→10 canary。
- **四池双探针盘点文档**：新增 [08-build-four-pool-dual-probe-todos-2026-07-22.md](../08-build-four-pool-dual-probe-todos-2026-07-22.md)。生产快照 `delete=0 / dispatch≈211 / normal=2`；主路径 Done；剩余 FP-001～015（选号去全表、额度序、DRR 观测、死 API 清理、时间轮二期等）写入 backlog BE-009～013。
- 更新 `04-improvement-backlog`：BE-005 → Dropped（被四池取代）；FE-003 备注指向四池面板。
- **Imagine 次数接口落地（本地，未部署）**：从当前 Grok SPA 确认 `GET /rest/usage/free-usage-gates`，解析字符串/数值形式的 `imagine.allowance/remaining`，写入独立 `mode=imagine` 窗口。接口不返回窗口秒数或绝对重置时间，代码不再猜“每日刷新点”。
- **纠正 `0/0` 语义**：3 个账号只读验证均 200，且历史 Lite 成功账号也返回 Imagine `0/0`；因此它表示免费闸门不适用或上限未知，而不是已耗尽。Selector 与图池仅在 `total>0 && remaining=0` 时阻断。
- **账号×模型状态持久化（本地，未部署）**：新增 `account_model_states`，主键 `(account_id, upstream_model)`；状态为 `unknown/quota_available/available/soft_stop/quota_exhausted/auth_failed/signature_failed`。成功、soft-stop、429 usage limit、401 和最终 anti-bot 403 分别落状态，重启后仍保留模型级排序。
- **图池调度改为 Imagine 独立证据（本地，未部署）**：不再借用聊天 fast 次数。真实成功优先、明确正额度待探测其次、`0/0`/未同步未知仍可路由；明确耗尽、认证/签名失败及活动 soft-stop 暂不入图池。Accounts API/页面新增次数与状态展示。
- **完整接续清单**：新增 [09-imagine-quota-model-state-and-10-concurrency-todos-2026-07-22.md](../../09-imagine-quota-model-state-and-10-concurrency-todos-2026-07-22.md)，逐项记录已完成代码、明确未完成项、上游无法提供的 ResetAt、Panda 发布/canary、10 个 fresh 账号门槛、`1→2→4→10` 验收指标、signer sidecar 与完成定义。

## 2026-07-21

- **文生图流水线 + 时序图**：`ImagePipelineScheduler`（10 槽 / 扩写 2 / SSE AIMD 2–6 / 下图 8）；账号 lease SSE 后早释；`ScopeWebExpand`；Admin `GET /api/admin/v1/image-timeline`；前端 `/image-timeline`；canary `GROK2API_GROUPS=2,4,10`。
- **Build 四池 + 双探针**：调度/普通/验证/删除；废除恢复池与 `retired:` 软退役；死号 `deletable:` 后维护探针物理删除；`GROK2API_BUILD_SAFE_PURGE_APPLY` 默认 true。热路径 `poolindex.DispatchIndex` + 三路到期堆 + DRR 5:3:2；Selector 按调度索引有序试租约。外挂 `tools/grok2api-account-pool-probe.py` 跳过 Build。
- **udeal + 零浏览器上线**：LA 口 `70.39.164.200:30000` 过 CF；Webshare 100/100 `cf_challenge` 不可作 `grok_web`。关 browser-bridge，本地 `zb` 签名器（nsenter 进 grok2api netns `:8788/sign`）现算 `x-statsig-id`。
- **g2a 直连验收**：Web 对话 ×3 / 文生图均 200；图生图链路通但易撞额度，模型已关。
- **调度收窄**：仅 10 号 `659,661,663,667,669,671,673,674,675,677` 进 `grok_web`；其余禁用；只开文生图（`grok-imagine-image`/`quality`），关 edit/video。
- **晚间运维**：`#105` BaseURL → `http://grok2api:8000`（`docker network connect grok2api_default new-api`）；调度池 **10→20**（+`641,642,644,646,647,649,650,652,654,656`）。
- **10 号刷额度** 10/10；单口带宽下行 ~15.5 Mbps、上行 ~1.54 Mbps；Lite 出图样本 mean **170KB** JPEG / **784×1168** → 调度改 `webConcurrency=2`、`assetConcurrency=8`、`mediaConcurrency=1`。
- **代理切分**：不可 udeal 握手 + webshare 上传；`grok_web`=质量口，`grok_web_asset`=CDN 下图可另挂。
- **NewAPI `#105` 文生图**：token 鉴权踩坑后修好（DB key 无 `sk-`、48 位无连字符、`group=grok`）；abilities `enabled=true`；`8081/v1/images/generations` **200**（约 7–11s，复测 7.4s），URL 指向 `grokimage.relai.asia`；可用 token 例：`group=grok` 的 id `920` / canary `930`。
- 文档：新增 [07-udeal-zero-browser-ops-2026-07-21.md](../07-udeal-zero-browser-ops-2026-07-21.md)；更新 current-state / backlog / pure-http AC。

## 2026-07-18

- **稳定性发现写入** [`docs/http-reverse-lite-chain.md`](../http-reverse-lite-chain.md)：成功靠 Playwright 自带 Chromium + `/rest/modes` 可回放票；`channel=chrome`/乱点 Cookie 会导致全 anti_bot；账号 86「年龄门」为误判（探针无「出生年份」DOM）；另见 modes `chat_ok` 后 Lite 仍可能间歇 403。
- **全自动画猫复验**：`tools/_cat_lite_once.py`，无人工；`/rest/modes` probe 200 → Lite 200 ×2；落盘 `.tmp/lite-images/cat-20260718-153100/final.jpg`。
- **最小出票开销**：`_chrome_sign_minimal.py` 耗时 ~9–10s（304）；本机 Chrome RSS 采样受其它标签干扰，增量约数百 MB；对照 Panda 历史 bridge **0.5–0.7GB**。结论：时间已压小，**内存硬底仍数百 MB**，2C/3.6G panda 仍不宜跑 Chrome；继续降本靠暖池/独立 worker/`headless-shell`，不能无浏览器出票。
- **最小出票原型**：`tools/_chrome_sign_minimal.py`（拦媒体/字体、早退 `/rest/modes`+cf）；账号 304 **9.5s** 出票成功（对比完整链出票段 ~32–38s）。结论：可独立成 sign 服务并降本，但**不能去掉 Chrome**；暖池单浏览器多号是进一步降本关键。
- **链路固化**：正式入口 `tools/web_chrome_http_lite_frozen.py`；指针 `web_http_chat_image_canary.py`；实现 `_chrome_channel_chat_image.py`；文档补「出票配置 / 票寿命与复用」（短用即弃，modes 票可同会话探针+一次 Lite）。
- **Free 档 + CPU 口径**：`panda_export_free_web_sso.py`（fast=30/auto=7）；账号 **400** Lite 成功。CPU 采样：Chrome 峰 ~115%单核（均 18%）、Python 峰 ~8%、主机峰 ~30%；内存 Chrome 峰 ~2.1GB（出票短签，含本机其它 Chrome）、Python ~142MB。澄清：2GB 不是「生图常驻」，是签票浏览器峰。
- **Chrome+HTTP Lite 冻结 + 两轮 bench**：文档改写 `docs/http-reverse-lite-chain.md`（正式路径=`channel=chrome`+`chrome146`）；脚本 `tools/_chrome_lite_bench.py`。成功轮：**661**（总 57.6s，sign 37.4 / lite 8.3，Chrome 峰 ~2.65GB，盘 ~213KB）+ **657**（总 60.3s，sign 32.2 / lite 9.3，Chrome 峰 ~2.06GB，盘 ~212KB）；663/659 因 curl TLS 抖动失败。额度可读：`POST /rest/rate-limits` → fast 29/30、window 86400s（ResetAt 为 now+window 推算）。产物 `.tmp/chrome-lite-bench-result.json`、`chrome-lite-bench-r4.json`。
- **Chrome+HTTP Lite 语义奶牛猫（黑白狸花/tuxedo）**：账号 **667**；prompt 明确 tuxedo / NOT cow hybrid + 文案写 16:9；chat/Lite 200；落盘 `.tmp/lite-images/chrome-lite-20260718-174232/final.jpg`。实测像素 **1168×784**（≈3:2），**Lite 不兑现 16:9**（REST 无 aspect 字段；真 16:9 仍须 Imagine WS）。
- **channel=chrome 出票 + 纯 HTTP chat/Lite 复验通过**：本机 Chrome 150 + Playwright `channel=chrome` 签票（含 `cf_clearance`）→ `curl_cffi chrome146` + UA Chrome/150；账号 **677**；`/rest/modes` probe 200 → chat 200 → Lite 200 ×2；落盘 `.tmp/lite-images/chrome-lite-20260718-173819/final.jpg`。脚本 `tools/_chrome_channel_chat_image.py`。
- **Panda Web SSO 导出**：`tools/panda_export_web_sso.py` → `ssh panda` 跑，本机 `.tmp/web-sso-canary.json` 已扩到 **25** 号（86–100、209–220）；勿再只啃本地旧 2 号。
- **Imagine 16:9/2K 奶牛猫**：入口 `tools/_imagine_once.py`（Lite 控不了分辨率）；轮换 SSO 后仍见 `Image rate limit exceeded` / 偶发 `ERR_CONNECTION_CLOSED`（出口侧），图未落盘。
- **冻结短签+纯 HTTP Chat/Lite 链路**：入口 `tools/web_http_chat_image_canary.py` → `.v1.py`；基线 `.tmp/web-http-chat-image-canary-baseline.json`。
  - 复跑（account 86）：A anti-bot ✓；B UI/`/rest/modes` 出票；C chat 200（3.57s）；D Lite 200（9.2s）×2 图。
- **本机纯 HTTP 对话+Lite 生图 PoC（BE-008 前半）通过**：业务请求不经 Chromium；浏览器仅短时出签名。未改生产 `doModelRequest` / 未上线 `http_reverse` 开关。
- 本机提示缓存 A/B（`tools/cache_ab_matrix.py`，`cache-ab-v1`）：环境就绪后跑满 baseline（7 case × 3 重复 = 42 请求，全 200）。
- 门禁 **REJECT**：`R-S` 显式 `prompt_cache_key` 粘滞 3/3 同账号，但 round2 `cached_input_tokens` 全 0；`M-S`/`M-R`/`R-R` 亦全 0。追加 24k 前缀 canary（input≈6199）仍双轮 cached=0。
- 结论：本机 Build 路径上「稳定 conv-id/prompt_cache_key → 必现缓存命中」假设不成立；按方案**不改**粘滞/随机 conv-id 逻辑、**不部署** Panda。产物：`artifacts/cache-ab/baseline/`、`gate_decision.md`、`large_prefix_canary.json`。
- 附带观察：Messages+`cache_control` 未出现账号粘滞（与 Responses 显式键对比），留作后续单独排查，不作为本轮改代码依据。

## 2026-07-17

- 生产部署（`cf12c4c` / `sha256:0360e4800d2a...`）：本机相关包测试 + `pnpm lint`/`pnpm build` → Actions `29562812320` success → Panda 仅 pull+重建主 API；`healthz` 本地/公网 200；bridge 未拉起；约 32 MiB / 0.09% CPU；`purgeApply=false`；池约 production 58 / retired 248。
- 探针集成安全删除门禁：验证/恢复池皆空时，仅扫描 `retired:`/`deletable:` 禁用号，串行「检查 → 刷新令牌 → 测模型」；成功复活进生产池，全失败先标 `deletable:`。`purgeApply` 默认关（面板 Switch + confirm + `PATCH /accounts/build-probe` + env `GROK2API_BUILD_SAFE_PURGE_APPLY`）；开启后需二次确认才删已标可删号（两阶段，防一次超时误删）。手动禁用（无 retired/deletable 前缀）不进候选。
- Dead 账号探测（`grok2api-dead-account-refresh-probe.py`）：275 个 disabled Build 中，`access_denied` 166 / `invalid_grant` 85 全失败；`other` 24 里 **18 个** OAuth+Chat 通（`observed_model=grok-4.5-build-free`）。先前被误标 `retired: ...403` 软退役，手动刷新不会自动 `enabled=true`。
- 已复活 18 个进生产池：`enabled=1` + `auth_status=active` + 清 `last_error`/cooldown/failure；备份 `backend-before-revive-capable-*.db`。IDs：`106,124,172,177,185,416,422,429,436,453,460,470,483,486,488,506,528,549`。池约 production **55**。
- 安全删除策略落地（避免误删可恢复号）：默认 dry-run；对候选先 **检查 → 刷新令牌 → 测模型**；三者都无返回才允许 `--apply` 删除。脚本：`tools/safe_purge_accounts.py`（宿主机 `/opt/grok2api/tools/`）。
- 账号页标题旁增加「刷新」按钮：一次失效并重拉列表、汇总、趋势与 Build 探针状态，避免整页浏览器刷新。
- 生产部署（`8961aa6` / `sha256:79cd27fec01a...`）：本机 `pnpm lint`+`pnpm build` 通过 → Actions `29559496097` success → Panda 仅 pull+重建主 API；`healthz` 本地/公网 200；bridge 未拉起；约 47 MiB / 0.10% CPU；旧默认摘要已切到新镜像。
- Build 循环探针增强：扫到哪个账号就对该号做条件凭据续期 + Billing 同步，再执行 grok-4.5 能力探测；Billing 失败只记日志不阻断；探针超时 45s→90s。
- Messages usage：Grok/OpenAI 风格的 `input_tokens`+`cached_tokens` 对外拆成 Anthropic 的 `input_tokens` / `cache_read_input_tokens`，避免 NewAPI 看不到缓存字段。
- Messages 请求：由 `cache_control` 派生的粘滞键写入上游 Responses `prompt_cache_key`（不覆盖调用方显式值），便于上游返回缓存命中。
- NewAPI 调度修复（运维）：`grok-4.5` 仅挂在 Anthropic 渠道 #101；#99/#100 分别只用 `grok-4.5-chat` / `grok-4.5-responses`，消除 Claude `/v1/messages` 抽到 xAI 渠道返回 `not available`。
- 对照证据：ccswitch 与 grok2api 审计对同请求 input≈100k+ 一致；此前 NewAPI 列表里 194 token 是连通性短测，大请求在 token `g` 下亦有 `prompt_tokens` 记录。缓存为 0 的主因是上游未返回 `cached_tokens`（现已注入 `prompt_cache_key` 等待观察）。
- 部署硬规则写入 playbook/current-state/charter：**禁止在 Panda 编译构建**；仅本机改测 → GitHub/GHCR → Panda `pull` → 按需清产物。
- 生产验收（`ff7f7ca` / `sha256:7b32bcb6f73c...`）：本机相关包测试通过 → Actions `29549850820` success → Panda 仅 pull+重建主 API；`healthz` 本地/公网 200；bridge 未拉起；探针约 30s/号；扫到的恢复号 Billing `synced_at` 与探针时刻对齐（如 445/448/456…），`build_probe_billing_refresh_failed=0`；池约 production 31 / recovery+quarantine 主导；旧镜像 `e64613c7...` 已删。
- 修复安全删除死循环：purge 强制刷新 Token 时 `UpdateTokens` 会清空 `deletable:`/`retired:`，导致同号反复打标永不物理删除并堵死队列（生产卡在 id=102）。现保留禁用号 purge 标记，并在进门禁前记住 alreadyMarked。

## 2026-07-16

- Build「待验证」含义已明确：账号凭据可读取且状态 active，但尚未成功完成最小 Build Chat 能力请求，因此不会进入生产可信池。
- 将 Build 探针从固定 5 分钟节拍改为自适应单并发循环：按 `priority DESC, id ASC` 顺序取号；有候选时每 30 秒处理 1 个，扫空后每 5 分钟巡检；临时失败账号冷却后再进入后续轮次，权限拒绝立即隔离。
- 新增配置边界和顺序扫描测试；Panda 部署继续要求资源预检、单账号 canary 和健康回归。
- GitHub Actions run `29483394620` 的后端测试、vet、Swagger、前端 lint/build、amd64/arm64 镜像发布均通过；Panda 部署摘要 `sha256:4ce09f388c12...`。
- Panda 部署前发现 browser-bridge 意外运行并占用 30% CPU / 652.6 MiB，已按资源门槛停止；部署只重建主 API，没有拉起浏览器。
- 生产验收：探针约每 30 秒处理 1 个，恢复池与待验证池交替且无并发重叠；池快照为 production 18 / verification ready 82 / cooldown 6 / quarantine-recovery 205；主服务 healthy，约 54 MiB，公网 `https://grokimage.relai.asia/healthz` 返回 200。
- 新增 Build 循环探针可视化：管理 API 暴露当前/最近账号、验证/恢复阶段、下次调度、运行期统计、七类池数量和最近 20 条结果；Accounts 页每 5 秒自动刷新并在完成一轮后同步账号列表。
- 失效分流沿用既有单并发状态机：权限拒绝立即隔离，前两次恢复失败分别进入 15 分钟/1 小时退避，第 3 次失败软退役；不在 Panda 管理页开放批量启动或并发调节。
- 本地验证：后端全量测试、前端 lint/build、管理 API、桌面页面和 390px 视口均通过；移动视口无横向溢出。
- 生产 canary 发现并修复软退役终态被延迟 OAuth 刷新失败覆盖的问题；`MarkReauthRequired` 现在不会改写已退役账号，回归测试覆盖“退役标记保留 → 重导入可复活”。
- GitHub Actions run `29501465000` 全部通过，Panda 更新至镜像摘要 `sha256:e64613c737c0...`；部署后主服务 healthy，约 49.6 MiB / 0.06% CPU，bridge inactive。
- 部署前按硬门槛清理 24 小时前未使用的 Docker build cache，根分区从 86% 降至安全范围，最终稳定在 49%；未删除运行镜像、卷或业务数据。
- 数据修复使用在线备份 `backend-before-retired-state-repair-20260716-212258.db`：1 条 canary + 45 条精确匹配记录；观察后 `remaining_overwritten=0`，探针继续按 30 秒单并发运行。
- 生产 API 快照：总账号 311，production 32 / verification 181 / cooldown 13 / quarantine 28 / recovery 5 / retired 52 / disabled 0；服务重启后的前 7 次探测均被正确记录，最近 20 条按账号去重验证无重复扫描。
- 修复“手动凭据刷新后仍显示待验证”：根因是旧流程只刷新 Token，不执行 Build Chat 能力验证。新流程在单号/批量刷新成功后立即探测并写入 `observed_model`；批量刷新限制为单并发，避免 Panda 突发请求。

- 用户在 `https://grokimage.relai.asia/accounts` 确认大量「待验证」，要求：①探活进度前端可视化；②解释 panda 直连 CF 403 以及 webshare 是否也 403；③推进 HTTP 逆向；④同步进 `docs/` 待办。
- Cloudflare 对照（plain HTTP）：panda 直连 `grok.com` → 403 Just a moment；Webshare 抽样 5 节点全部同样 403+CF。结论：当前机房代理并非「直连才被拦」。
- **udeal 抽样（Desktop `udeal1000proxy.txt`，panda 上测）**：转成 `as.udealproxy.com:6666` 粘滞会话后测 80 条 → 出口可达 71；`cf_challenge` 71；**plain HTTP `ok_200` = 3**（session `QmFKV`/`ff4G`/`qQtT`）。`QmFKV` 复核为真实 Grok 首页（`<title>Grok</title>`，419KB），egress `103.125.42.96`（ID / MyRepublic）。相对 Webshare 的 0/5，udeal 有可用苗头，但仍需筛池 + 浏览器 canary。
- 空代理 egress 生图仍 502（bootstrap ~103s）；有请求时 bridge 峰值约 CPU 76% / 内存 487MB。
- 对照 `/root/gptimage`：生图走 `curl_cffi` HTTP，浏览器仅清障；列入 BE-008。
- 文档：新增 [06-open-todos-2026-07-16.md](../06-open-todos-2026-07-16.md)；更新 backlog FE-003/BE-007/BE-008/MTN-007/MTN-008、current-state、roadmap。
- 未在本轮实现前端探活面板或完整 HTTP 逆向代码（先落待办与证据）。
- **用户纠正**：本机 `203.10.99.34` 是机房 IP（GSL AS137409），不是家宽；不能用「家宽 vs 机房」解释本机 200 / panda 403。
- **CF 拒因深挖**（`grok-cf-deep-diagnose.py` + 新鲜 40 条 udeal）：
  - 被拒主因 = **Managed Challenge**：`cf-mitigated: challenge` + 正文 `cType: 'managed'`；非 Turnstile、非「You have been blocked」硬封文案。
  - 本机 pass：无 `cf-mitigated`，有 `cf-cache-status: DYNAMIC`，真 `<title>Grok</title>`。
  - 早盘 ok 的 QmFKV/ff4G/qQtT 深挖时 **全部变 challenge**（粘滞出口漂移）。
  - 新鲜 40 条：`pass_app=1`（`6XIJ`→`82.47.112.28` AS402289 Webgist/GB，PoP MRS，复测仍 200）；challenge 31；connect_fail 6；other_403 2。

## 2026-07-15

- 目标：完善图片管理、降低 Web 并发风险并处理 107 个 Build 异常账号。
- 观察到的事实：Panda 单 Webshare 节点连接正常，Grok 返回 Cloudflare 403；Build 异常由 87 个 `invalid_grant` 和 20 个 `access denied` 组成，只有 2 个 invalid_grant 账号已有 Web 关联。
- 已完成：图片日期筛选/分组/按日删除/全部删除；秒级时间、生成耗时、实际和请求分辨率；Web/Web Asset 单并发闸门；已关联失效 Build 的 Web→Build 修复路径；账号状态操作提示；维护文档实化。
- 验证：`cd backend; go test ./...` 通过；`cd frontend; pnpm lint` 与 `pnpm build` 通过；本地临时实例验证图片列表、日期筛选、图片读取、元数据和按日期删除通过；Panda 按资源门禁完成单服务部署、只读状态分析和单代理请求 canary。
- 生产结果：镜像 `sha256:80d6ab8d5922...` 已部署，页面显示日期分组、秒级时间和旧图片实际尺寸；Panda 部署后主服务约 28–38 MiB，持续 healthy。
- Build 结果：2 个已有 Web 关联的 invalid_grant 账号已成功换取新凭据，但能力探测仍返回 Build Chat access denied；最终准确状态为 80 active、85 invalid_grant、30 access denied。
- Web canary：Webshare 连接正常，但单浏览器加载 Grok 页面超时，后端 502；30 秒启动上限、阶段错误和结构化 502 已部署。空载桥接容器通过内部健康检查后重新停止，未运行浏览器会话或全量刷新。
- 未完成：剩余 Web 账号仍受 Cloudflare 出口会话阻塞；85 个 Build 账号等待新 RT，30 个等待具备 Build 权限的授权。
- 下一步：更换住宅/ISP 出口后再做单账号 canary；导入新的有效 RT 或具备 Build Chat 权限的授权。
- 关联文件：`backend/internal/application/media/`、`backend/internal/infra/egress/`、`backend/internal/application/account/`、`frontend/src/features/media/`、`frontend/src/features/accounts/`。
### Messages、Build 可信池与 Webshare 深度诊断

- 复现 NewAPI Messages 间歇性 403：请求正确进入 Build Messages 渠道，但所选 active 账号被上游返回 `permission-denied`；Claude Code 将下游 403 误解释为需要 `/login`。
- 修复 Build 权限拒绝后的错误恢复：不再强制刷新 RT；Token 更新不会把 `chat endpoint access denied` 账号重新置为 active。
- 生产选号增加可信池保护：存在成功观测型号时，仅使用已验证 Build 账号，未验证账号不再由用户流量试错。
- 修复 Chat/Messages 对外模型名：公开返回 `grok-4.5`，内部继续保留真实观测型号。
- 修复 Anthropic Messages 流式 usage：合并 `message_start`/`message_delta`，记录输入、输出、缓存命中，并为 `cache_control` 稳定前缀派生粘滞键。
- 永久 Build 权限拒绝对外改为 503；Anthropic 使用 `overloaded_error`，避免客户端提示重新登录。
- Webshare 诊断确认：100 个 IP 仅覆盖 10 个 `/24`，均为新加坡机房 ASN；Panda 代理鉴权可用但 Grok Cloudflare 会话被拒绝，问题不是白名单或单纯并发。
- 本机验证：受影响包测试、`go test ./...` 与 `go build ./cmd/grok2api` 均通过。
- 追加 Build 可信池扩展：重新导入异常账号会清除旧观测型号；后台按 5 分钟 1 个的节奏发起最小 Responses 能力探测，避免生产流量试错。
- 生产部署：镜像 `sha256:7901d845009c...` healthy，资源限制保持 0.75 CPU / 512 MiB；首个串行探测确认账号 147 无 Build Chat 权限并正确隔离。
- NewAPI Messages 端到端 canary 返回 200，公开/上游模型均为 `grok-4.5`，最终 usage 输入 198、输出 24。
- NewAPI 图片渠道及 abilities 派生表已备份并同步为纯 Grok 名称；由于 Web Cloudflare 会话仍失败，渠道保持 disabled，`/v1/models` 不再返回旧 `gpt-image-grok-*` 别名。

### 多池恢复调度与浏览器身份连续性

- Build 权限拒绝继续立即隔离；原有 5 分钟单账号 worker 扩展为恢复池/待验证池交替调度，总并发仍为 1。
- 恢复顺序为关联 Web SSO、一次 RT 旋转、最小 Build Chat 探测；失败按 15 分钟、1 小时、6 小时退避，3 次后软退役，不由后台执行不可逆硬删。
- 管理 API/页面新增生产、待验证、隔离、恢复、退役池标识、恢复次数和下次恢复时间；重导入有效凭据可复活软退役账号。
- Panda 部署新账号池后，单并发恢复 worker 已确认 `invalid_grant` 不会重复旋转旧 RT，而是进入退避等待新凭据。
- Web 深挖确认 33 个 `grok_web` 出口全部因历史 `transport error` 冷却，额度请求在出口选择阶段 15ms 内失败，未到达浏览器；根因是桥接停机错误污染了代理健康。已增加桥接控制面错误分类，桥接不可达不再冷却代理，并将桥接硬限制降为 0.75 CPU / 768 MiB。
- 部署复核发现 compose 命名卷为空，而生产完整库仍位于 `/opt/grok2api/data/backend.db`；已恢复项目目录 bind 挂载并把仓库 compose 固化为 `${GROK2API_DATA:-./data}`，避免后续项目名切换造成空库启动。
- 低资源调度固定为 Web 启动不扫全池、每 30 分钟 1 个，Build 每 5 分钟 1 个；修复关闭 Build worker 被 supervisor 每 30 秒重启刷日志的问题。
- 进一步比对确认 Panda 宿主机桥接代码哈希与 `chrome146` 镜像内代码不同，先前 UA/CDP 与会话复用修复未进入实际容器。Compose 已增加 `/bridge/app.py` 只读 bind，避免为单文件更新在 Panda 构建大型 Chrome 镜像。
- 桥接密钥路径也曾因 `.env` 丢失而回退到不存在的默认路径，Docker 将其创建成目录，导致桥接认证恒为 401。已恢复 `/root/.secrets/grok2api-browser-bridge-key-main`，主服务、桥接和宿主机哈希一致；Compose 禁止自动创建缺失密钥路径。
- 认证修复后的真实 Chromium canary 运行 12.32 秒，随后 Panda load1 达 2.10，资源监控按硬门槛停止桥接；主服务保持健康，33 个 Web 出口健康度未再被污染。后续应迁移独立浏览器 worker，不在 Panda 放宽门槛。
- Web 根因审计发现生产配置 `BRIDGE_REUSE_SESSIONS=false` 且桥接忽略出口 UA；已改为会话复用 30 分钟，并同步 UA 与平台，代理/Cookie/UA 共同参与会话键。
- Panda 只读预检确认桥接两份密钥哈希一致；主服务 healthy、约 57 MiB，桥接停止。未启动 Chrome、未刷新账号。
- 验证：后端 `go test ./...`、前端 lint/build、浏览器桥接 13 项单元测试均通过；待提交 CI 和 Panda 单账号 canary。

## 2026-07-26 票机制失效性验证 + asset403 定位 + 硬门禁改软

- 生产只读盘点发现图轨处于硬死：票池 07-25 19:50Z 因 12h TTL 自然腐烂归零（非消耗殆尽，归零时仍有 19 张从未使用的票），而生产侧无任何灌票进程（无 cron/timer/进程），铸票强依赖开发者本机 Chrome。
- 追代码确认票只投递两样东西：`statsig_meta` 传给 signer 后被丢弃（signer 只用启动锁定 pair，190 张票 179 个不同 meta 与 signer 的均不同），`device_cookie` 里真正有效的只有 `grok_device_id` 与 `x-userid`。
- 新增 `tools/http_mint_probe.py`：一次带 SSO 的首页 GET（curl_cffi，走 udeal）即拿到 `grok_device_id` + `x-userid` + meta48，2.08s。自铸 `x-userid` 与该账号历史 Chrome 票逐字节相同。推入生产票池后走真实 API 生图 **200 / 14s**，`sign_source="http_mint_poc"`。**「生产侧没有铸票能力」这一 BE-024 前置阻塞不成立。**
- 无票裸跑手动 HTTP 链路 **3/3 出图**（chat 200、asset 200、147KB/180KB/237KB JPEG，`anti_bot=false`）。
- 据此把 selector 硬门禁改软：`filterChromeTicketCandidates` 无持票候选时回退原候选集而非返回 `SelectionNoChromeTickets`，保留有票优先；移除会误报的 503 归因分支与随之无用的 `chromeTicketsEnforced()`。测试改为 `TestSelectorPrefersChromeTicketHoldersAndFallsBackWhenPoolEmpty`。`go test ./...` 46 包全过。
- 同批回流一处**只存在于生产二进制、未进仓库**的修复：`settings/service.go:306` 整体替换 `base.Routing` 时漏掉 `DisableCooldown`，会静默重置为 false；改为保留既有值。
- 部署后验证：票池 available=0 时生图**不再 503**，请求进入上游且图已生成，改卡在 asset 下载 403 → 502。
- asset 403 逐项排除后定位到 **Go 的 `bogdanfinn/tls-client` 指纹**：node 110/111 同 IP（排除出口差异）；仅 sso 与 sso+CF warm 下载都是 200（排除 CF cookie）；请求头与 cookie 格式两侧一致。决定性证据是取 Go 刚 403 六次的同一 URL，用 curl_cffi 同代理同 cookie 同 UA 下载得到 **200 / 205KB JPEG**。两侧都声称 Chrome 146，但只有 curl_cffi 被 `assets.grok.com` 放行。
- 附带发现：`rewarmAssetDownloadCookie` 在无 deviceCookie 时直接返回，无票路径完全跳过 CF 重warm，6 次重试形同虚设。
- 事故：`scp` 传二进制丢失可执行位，`docker cp` 后容器重启循环，停机约 66 秒（08:48:06Z→08:49:12Z）。已 `chmod 755` 后恢复；替换前原件存档 `/opt/grok2api/binary-archive/grok2api-prod-20260726-preSoftGate`。后续 `docker cp` 前必须补可执行位。
- 结论档：[21-ticket-obsolescence-and-asset403-tls-2026-07-26.md](../../21-ticket-obsolescence-and-asset403-tls-2026-07-26.md)。
